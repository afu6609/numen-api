// Loader plugins are declared by their own subprojects. Keeping Loom out of the
// root project lets -Pforge_only configure and publish the dedicated-server
// artifacts without resolving a Fabric-only plugin.
